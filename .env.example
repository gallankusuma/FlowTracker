# Template for root-level utility scripts (check_db.js, fix_db.js, etc).
# Copy to .env and fill in real values — never commit or archive the real .env.
# See scraper/.env.example for the full variable list used by the backend.

DB_HOST=localhost
DB_USER=erp_user
DB_PASSWORD=
DB_NAME=erp_manufacturing
