# ── BACKEND ──
PORT=3001
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
NODE_ENV=production

# ── DATABASE ──
# SQLite path (relative to backend/)
DB_PATH=./flowtracker.db

# ── CORS ──
# Frontend URL for CORS whitelist
FRONTEND_URL=http://localhost:5173
